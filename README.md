# flight_booking
go inside the `src` folder and run the below command 
```
npx sequalize init
```
by executing the following command you will get the migration and seeders folder along with a config.json file inside your config folder
-if you are setting up your development environment write the  username , password, database_name , dialect mention whatever db do you have for example mysql,postgres etc
-if you are using the test and prod enviromnent make sure you replce the hosted db url 

